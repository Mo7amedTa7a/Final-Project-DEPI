rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ WARNING: These rules allow public read/write access
    // This is for development/testing only. For production, implement proper authentication!
    
    // Allow read/write access to all collections (for development)
    match /{document=**} {
      allow read, write: if true;
    }
    
    // TODO: Implement proper security rules for production:
    // - Use Firebase Authentication
    // - Restrict access based on user roles
    // - Example:
    //   match /users/{userId} {
    //     allow read, write: if request.auth != null && request.auth.uid == userId;
    //   }
  }
}

